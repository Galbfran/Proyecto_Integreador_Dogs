/* const Dog = sequelize.models;

const obtenerDogsBD = async () => {
    try {
        const perros = await Dog.findAll();
        return perros;
    } catch (error) {
        throw new Error(error.message);
    }
}

module.exports = obtenerDogsBD; */